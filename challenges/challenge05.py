# Create a script that reads an integer number and shows its successor and its predecessor. 
num = int(input('Type a number: '))

successor = num + 1
predecessor = num - 1

print('Number: {}, Successor: {}, Predecessor: {}'.format(num, successor, predecessor))
